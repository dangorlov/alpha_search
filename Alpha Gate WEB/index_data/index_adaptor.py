def Find(query):
    '''

    :param query: string, which describes a query
    :return: results: list like [["name", 'url', 'description']]
    '''
    results = [["name", 'url', 'description']]
    return results